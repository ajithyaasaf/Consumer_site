/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables React Strict Mode (helps identify potential problems in the app)
  swcMinify: true, // Enables SWC-based minification (default in Next.js, better performance)
}

export default nextConfig
